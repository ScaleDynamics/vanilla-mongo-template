{
  "name": "vanilla-app",
  "version": "1.0.0",
  "description": "",
  "main": "warp.config.js",
  "scripts": {
    "postinstall": "cd ../../backend/hello/ && npm install",
    "prestart": "warp project select",
    "start:client": "live-server ./src",
    "start:server": "warp dev ../../backend/hello/",
    "start": "run-p start:*",
    "build": "warp build ../../backend/hello/",
    "deploy": "warp deploy ./src ../../backend/hello/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5",
    "warp": "^4.0.7"
  }
}
